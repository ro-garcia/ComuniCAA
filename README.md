# ComuniCAA

Prototipo frontend en Flutter para una aplicación de Comunicación Aumentativa y Alternativa.

## Ejecutar

```bash
flutter pub get
flutter run
```

La app usa `provider` para estado, `flutter_svg` para pictogramas incluidos y `image_picker` para cámara/galería en pictogramas personalizados.
